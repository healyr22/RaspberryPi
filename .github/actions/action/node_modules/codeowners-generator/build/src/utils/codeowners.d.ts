export declare type ownerRule = {
    filePath: string;
    owners: string[];
    glob: string;
};
export declare const createOwnersFile: (outputFile: string, ownerRules: ownerRule[]) => Promise<void>;
export declare const loadCodeOwnerFiles: (dirname: string, files: string[]) => Promise<ownerRule[]>;
export declare const loadOwnersFromPackage: (dirname: string, files: string[]) => Promise<ownerRule[]>;
