import { Command } from '../utils/getGlobalOptions';
import { ownerRule } from '../utils/codeowners';
declare type Generate = (options: GenerateInput) => Promise<ownerRule[]>;
declare type GenerateInput = {
    rootDir: string;
    verifyPaths?: boolean;
    useMaintainers?: boolean;
    includes?: string[];
};
export declare const generate: Generate;
interface CommandGenerate extends Command {
    output?: string;
    verifyPaths?: boolean;
    useMaintainers?: boolean;
    includes?: string[];
}
export declare const command: (command: CommandGenerate) => Promise<void>;
export {};
